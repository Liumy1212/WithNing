# WithNing 系统公网部署指南

## 概述

本文档提供将 WithNing 打卡监督系统部署到公网的完整指南。由于您的数据库在本地，需要使用内网穿透技术来实现公网访问。

## 部署方案

由于数据库在本地，推荐以下部署方案：

### 方案一：使用内网穿透工具（推荐）

#### 步骤 1：配置本地数据库允许远程连接

1. 连接 MySQL 数据库
2. 执行以下 SQL 语句：

```sql
USE mysql;

-- 创建远程用户（将 'your_password' 替换为您的密码）
CREATE USER IF NOT EXISTS 'remote_user'@'%' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON withning.* TO 'remote_user'@'%';
FLUSH PRIVILEGES;

-- 或者修改现有 root 用户允许远程访问
UPDATE mysql.user SET Host='%' WHERE User='root' AND Host='localhost';
FLUSH PRIVILEGES;
```

3. 修改 MySQL 配置文件（my.ini 或 my.cnf）：

```ini
[mysqld]
bind-address = 0.0.0.0
port = 3306
```

4. 重启 MySQL 服务

#### 步骤 2：安装内网穿透工具

推荐使用 `ngrok` 或 `frp`：

**使用 ngrok：**
1. 下载 ngrok: https://ngrok.com/download
2. 注册账号并获取 authtoken
3. 配置 authtoken：`ngrok config add-authtoken YOUR_TOKEN`
4. 启动 MySQL 穿透：`ngrok tcp 3306`
5. 记录分配的地址（如：2.tcp.ngrok.io:12345）

**使用 frp：**
1. 下载 frp: https://github.com/fatedier/frp/releases
2. 配置 frps.ini（服务器端）
3. 配置 frpc.ini（客户端，指向您的电脑）
4. 启动 frpc 连接

#### 步骤 3：修改后端配置

编辑 `Backend/WithNing/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://YOUR_NGROK_MYSQL_HOST:PORT/withning?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: remote_user
    password: your_password
```

#### 步骤 4：部署后端到云服务器

1. 将构建产物上传到云服务器：
   - 后端 jar: `Backend/WithNing/target/WithNing-0.0.1-SNAPSHOT.jar`
   - 前端 dist: `Frontend/vue-frontend/dist/`

2. 在云服务器上安装 Java 运行环境（JRE 21+）

3. 启动后端服务：

```bash
java -jar WithNing-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod
```

或者指定数据库连接：

```bash
java -jar WithNing-0.0.1-SNAPSHOT.jar \
  --spring.datasource.url=jdbc:mysql://YOUR_NGROK_MYSQL_HOST:PORT/withning \
  --spring.datasource.username=remote_user \
  --spring.datasource.password=your_password
```

#### 步骤 5：部署前端

**使用 Nginx 部署前端：**

1. 安装 Nginx

2. 配置 Nginx（/etc/nginx/conf.d/withning.conf）：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 或服务器 IP

    location / {
        root /path/to/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

3. 重启 Nginx：`sudo systemctl restart nginx`

#### 步骤 6：配置前端 API 地址

编辑 `Frontend/vue-frontend/.env.production`：

```env
VUE_APP_API_BASE_URL=http://YOUR_SERVER_IP/api
```

重新构建前端：`npm run build`

---

### 方案二：将数据库也迁移到云服务器（推荐用于生产环境）

1. 在云服务器上安装 MySQL
2. 导入数据库：执行 `Backend/WithNing/src/main/resources/init.sql`
3. 修改后端配置连接云数据库
4. 按方案一的步骤 4-5 部署前后端

---

## 文件位置

- **后端 JAR 包**：`Backend/WithNing/target/WithNing-0.0.1-SNAPSHOT.jar`
- **前端构建产物**：`Frontend/vue-frontend/dist/`
- **数据库初始化脚本**：`Backend/WithNing/src/main/resources/init.sql`
- **生产环境配置示例**：`Backend/WithNing/src/main/resources/application-prod.yml`

## 测试账号

- 管理员：用户名 `admin`，密码 `123456`
- 用户1：用户名 `user1`，密码 `123456`
- 用户2：用户名 `user2`，密码 `123456`

## 安全建议

1. **数据库安全**：
   - 使用强密码
   - 限制 IP 访问白名单
   - 定期备份数据库

2. **服务器安全**：
   - 配置防火墙规则
   - 使用 HTTPS（配置 SSL 证书）
   - 定期更新系统补丁

3. **应用安全**：
   - 修改默认测试账号密码
   - 生产环境关闭调试模式
   - 配置合适的 session 超时时间

## 常见问题

### 1. 数据库连接失败
- 检查 MySQL 是否允许远程连接
- 检查防火墙是否开放 3306 端口
- 验证用户名密码是否正确
- 检查内网穿透是否正常工作

### 2. 前端无法访问后端 API
- 检查后端服务是否正常运行（端口 8080）
- 检查 Nginx 代理配置是否正确
- 检查 CORS 配置是否允许跨域
- 查看浏览器控制台具体错误信息

### 3. 前端页面空白
- 检查是否正确配置了 API 地址
- 检查 Nginx 是否正确指向 dist 目录
- 查看浏览器控制台是否有 JS 加载错误

## 技术支持

如有问题，请检查：
1. 后端日志输出
2. Nginx 错误日志（/var/log/nginx/error.log）
3. 浏览器开发者工具 Network 和 Console 面板
